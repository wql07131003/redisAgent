#include <sys/types.h>
#include "frman_include.h"

int query(int count, const u_int64_t* pids, delpost_record_t* records, 
		const char* mask_dir, const char* index_file_path, const char* mask_file_path);

